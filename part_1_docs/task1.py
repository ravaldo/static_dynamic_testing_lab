class CardGame:


  def check_for_ace(self, card):
    if card.value = 1:	                 # assignment operator used instead of equality
      return True
    else
      return False
   

  dif highest_card(self, card1 card2):   # should be "def" and also missing a comma
  if card1.value > card2.value:          # method body is not indented
    return card                          # should be card1
  else:
    return card2
  


def cards_total(self, cards):            # indentation; this method is not part of the CardGame class
  total                                  # variable is declared but initialised
  for card in cards:
    total += card.value
    return "You have a total of" + total
